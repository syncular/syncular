# bench — curated results

Generated by `bun run bench` on 2026-07-03T21:10:36.847Z (Bun 1.3.14,
darwin/arm64). Deterministic seeded row data; timings
vary run to run.

## SQLite blob-state simplification (2026-09-11)

Two independent collections compared the simplified blob state machine with
0.18.0. Each collection ran ten paired 500,000,000-byte attempts per core with
alternating arm order, fresh writer and reader processes, WAL/FULL client
databases, a distinct pinned MinIO container per attempt, and full SHA-256
receipts. All 80 attempts completed.

| Collection | Core | 0.18 upload | Candidate upload | Paired change (95% interval) | Cache-hit change (95% interval) |
| --- | --- | ---: | ---: | ---: | ---: |
| First | TS | 1,905.12 ms | 1,454.29 ms | -21.07% [-31.11%, -9.12%] | -61.60% [-63.84%, -58.03%] |
| Confirmation | TS | 1,994.98 ms | 1,524.89 ms | -24.93% [-32.73%, -17.18%] | -61.86% [-65.85%, -56.72%] |
| First | Rust | 2,147.38 ms | 1,993.12 ms | -8.99% [-13.66%, -4.70%] | -66.07% [-66.49%, -65.57%] |
| Confirmation | Rust | 2,212.32 ms | 2,027.48 ms | -6.80% [-10.00%, -3.44%] | -67.03% [-67.85%, -66.15%] |

The candidate stores upload state outside the immutable body row, writes
commit dependencies directly with the outbox commit, and reads live references
only during cache retention. It removes stored refcounts, four triggers,
startup backfill, reconciliation, and cache-hit writes. A fresh download
checkpoints its completed body from WAL before returning. Every candidate
download had a zero-byte WAL before the cache-hit measurement.

Rust fresh downloads improved in both collections. TS fresh-download point
estimates improved, but both intervals included zero, so the result supports no
TS fresh-download claim. Reopened cache hits improved in both cores and both
collections. Compact observations and the analyzer are under
the local ignored `bench/results/blob-simplification-v1/` directory; this file
retains the curated result.

## SQLite blob download reconciliation in 0.18.0 (2026-09-11)

The file profile downloaded one 65,536-byte object into a persistent client
with 100,000 visible `blob_ref` values. Each core ran ten paired baseline and
candidate blocks through MinIO, then repeated the complete collection with new
containers. The operation clock includes transport, SHA-256 validation, SQLite
cache insertion, reference reconciliation, cache-cap enforcement, and complete
byte materialization. Fixture creation and final digest validation stay outside
the clock.

| Collection | Core | Baseline median | Candidate median | Paired change (95% interval) | Median saving |
| --- | --- | ---: | ---: | ---: | ---: |
| First | TS | 48.68 ms | 18.78 ms | -61.72% [-63.70%, -59.10%] | 30.56 ms |
| First | Rust | 38.42 ms | 15.75 ms | -59.55% [-61.56%, -57.56%] | 22.49 ms |
| Confirmation | TS | 49.31 ms | 18.97 ms | -61.57% [-63.13%, -59.83%] | 29.82 ms |
| Confirmation | Rust | 39.01 ms | 14.84 ms | -60.05% [-61.70%, -57.99%] | 23.67 ms |

The candidate asks SQLite to count references for the downloaded body and
updates only that cache row. The baseline selected and parsed every visible
reference, reset every cached refcount, and rewrote the complete count map.
Both confirmation intervals exclude zero, and both median savings exceed the
predeclared 64 KiB A/A floors: 3.267 ms for TS and 10.705 ms for Rust.
Staging, upload plus commit, cache-hit, and reopened-hit confirmation intervals
include zero.

**Lane**: bun:sqlite loopback — the TS client core and the real
server library exchange in-process SSP2 bytes (transport, segment download
and realtime are direct function calls / RealtimeHub callbacks). Same
philosophy as the conformance loopback: no sockets, no HTTP stack, no
browser — and the client runs on bun:sqlite, not sqlite-wasm. Read every
number as the in-process best case: a real deployment adds network and
serialization costs this lane does not have.

## a. Bootstrap wall time (fresh client → fully-applied local rows)

| Case | Median | Rows/sec | Runs |
|---|---|---|---|
| 1k rows (rows lane) | 4.7 ms | 213,552 | 7 |
| 100k rows (rows lane) | 362.6 ms | 275,774 | 5 |
| 100k rows (**sqlite image**, §5.3) | 30.4 ms | 3,293,979 | 5 |

The image lane's median is the warm/storm case: the server reuses the
stored image per (scopes, pin) — §5.3's build-once rule — so every
client after the first downloads and imports at file-copy speed. The
cold first bootstrap, which also builds and stores the image
server-side, took 288.5 ms. The rows lane uses no precomputed artifact
at all: segments are scanned, encoded, and hash-verified per bootstrap.

All runs (ms), after one discarded warm-up:
- 1k rows lane: 4.8, 4.8, 4.7, 5.6, 4.2, 4.3, 4.2
- 100k rows lane: 368.2, 362.6, 364.7, 356.9, 357.0
- 100k image lane: 33.0, 30.8, 30.4, 30.3, 30.3 (cold 288.5)

## b. Online propagation (RealtimeHub, 2 clients, 200 iterations)

Measured from `mutate()` on client A to client B's post-apply ack of the
commit (B applies the binary delta to its local DB, then acks — §8.2).

| Metric | Measured |
|---|---|
| Propagation p50 | 0.1 ms |
| Propagation p95 | 0.2 ms |
| Write-ack p50 (A's push+pull round) | 0.1 ms |
| Write-ack p95 | 0.2 ms |

0.2 ms p95 is loopback-fast; over a real socket expect network latency
to dominate.

## c. Server memory (100k bootstrap)

| Metric | Measured |
|---|---|
| Peak RSS delta during 100k bootstrap | 20.0 MB |

Caveat: server and client share one process in this lane, so the delta
includes the client's local SQLite writes too — it is an upper bound on
the server share. Sampled at ~2 ms.

## Bundle size (browser client)

`bun build --minify --target=browser` of a minimal entry importing
`SyncClient` + the sqlite-wasm database backend. The `sqlite3.wasm`
binary is external (fetched at runtime), measured as shipped by
`@sqlite.org/sqlite-wasm`.

Bundle bytes refreshed on 2026-07-17 with the same deterministic build command.

| Artifact | Raw | Gzip | Whose bytes |
|---|---|---|---|
| syncular client code (core + codec) | 105.2 KB | 31.3 KB | **ours** |
| sqlite-wasm JS glue | 214.9 KB | 65.3 KB | vendor (SQLite) |
| sqlite3.wasm (external asset) | 844.5 KB | 396.1 KB | vendor (SQLite) |
| **Total** | **1164.5 KB** | **492.7 KB** | |

The size budget gates OUR bytes (decision 2026-07-03): vendor engine
bytes don't gate — every wasm-SQLite product ships the stock SQLite
distribution. Total reported for context: 1164.5 KB raw / 492.7 KB gzip.
