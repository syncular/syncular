#!/usr/bin/env bash
# Pre-push gate: the root check (typecheck + lint + tests incl. the isolated
# multi-tab lane). Cargo/bindings/bench gates run in CI (ci.yml).
set -euo pipefail
echo "[pre-push] bun run check..."
bun run check
echo "[pre-push] All checks passed."
