/** Protocol constants (SPEC.md §1.2, §9 are normative). */

/** SSP2 envelope wire version this codec implements. */
export const PROTOCOL_WIRE_VERSION = 1;

/** Magic bytes opening every v2 sync envelope (SPEC.md §1.2). */
export const SYNC_PACK_MAGIC = 'SSP2';
