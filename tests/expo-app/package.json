{
  "name": "@syncular/tests-expo-app",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "start": "expo start",
    "android": "expo start --android",
    "ios": "expo start --ios",
    "web": "expo start --web",
    "tsgo": "tsgo --noEmit",
    "start:dev-client": "expo start --dev-client --localhost",
    "ios:build": "expo run:ios --no-install --no-bundler --no-build-cache --device \"iPhone 15\"",
    "maestro:test": "maestro test ./maestro"
  },
  "devDependencies": {
    "@syncular/dialect-expo-sqlite": "workspace:*",
    "expo": "~54.0.33",
    "expo-sqlite": "^16.0.10",
    "expo-status-bar": "~3.0.9",
    "expo-system-ui": "^6.0.9",
    "expo-updates": "^29.0.16",
    "kysely": "*",
    "react": "19.2.4",
    "react-native": "0.83.1",
    "@types/react": "~19.2.13",
    "typescript": "~5.9.3"
  }
}
