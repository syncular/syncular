{
  "name": "@syncular-load",
  "private": true,
  "type": "module",
  "scripts": {
    "load": "bun run src/index.ts",
    "load:smoke": "bun run src/index.ts --smoke-sweep",
    "test": "bun test src"
  },
  "dependencies": {
    "@syncular/core": "workspace:*",
    "@syncular/server": "workspace:*",
    "@syncular/server-hono": "workspace:*"
  }
}
