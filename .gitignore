# The archived v1 tree lives on disk only (promotion 2026-07-04); its full
# history is in git. Delete the directory when the sunset completes.
v1/

.context/
.claude/
.ignore/
node_modules/
.DS_Store
dist/
.cache/
.turbo/
.tmp/
.env
**/*.log
# bun-built bundle emitted by `web-client verify:node` (Node-run adapter proof)
**/.verify-node.built.mjs
CLAUDE.md
WARP.md
coverage/
target/
**/*.af~lock~

# astro build cache
apps/docs/.astro/

# wrangler local state
apps/docs/.wrangler/
