{
  "name": "@syncular/console-app",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "dev": "bun ./index.html",
    "build": "rm -rf dist && bun run build.ts",
    "pub": "bun run build && wrangler deploy",
    "tsgo": "tsgo --noEmit"
  },
  "dependencies": {
    "@sentry/react": "^10.47.0",
    "@syncular/client": "workspace:*",
    "@syncular/console": "workspace:*",
    "react": "^19",
    "react-dom": "^19"
  },
  "devDependencies": {
    "@syncular/config": "workspace:*",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "bun-plugin-tailwind": "^0.1.2",
    "tailwindcss": "^4.2.2",
    "wrangler": "*"
  }
}
