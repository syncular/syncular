---
title: Rust Client
description: Rust-first Syncular client docs for Rust, browser TypeScript, Swift, Kotlin, and JVM apps.
---

# Rust Client

This section covers the Rust-first Syncular client. It is separate from the
existing JavaScript client docs on purpose: the Rust client owns SQLite, sync
state, outbox, conflicts, blobs, CRDT persistence, encryption, and native
bindings.

The current public shape is:

- Rust reads use Diesel query-builder semantics.
- Browser TypeScript reads use Kysely over a Rust-owned SQLite Worker.
- Swift and Kotlin use generated small query DSLs over generic native bindings.
- Writes go through generated Syncular mutations, not raw app-table SQL.
- App-specific code is generated from migrations plus `syncular.codegen.json`.

<Cards>
  <Card title="Quick Start" href="/docs/rust-client/quick-start" description="Generate app clients from migrations and schema config" />
  <Card title="Browser TypeScript" href="/docs/rust-client/browser" description="Use Kysely against Rust-owned SQLite in a Worker" />
  <Card title="Native Apps" href="/docs/rust-client/native" description="Use Rust, Swift, Kotlin, Android, and JVM bindings" />
  <Card title="Testing" href="/docs/rust-client/testing" description="Use the Rust testkit instead of mocking Syncular" />
  <Card title="Packaging" href="/docs/rust-client/packaging" description="Build local Apple, Android, and JVM native packages" />
</Cards>

## What stays generic

Low-level runtime and binding APIs are schema-agnostic. They expose SQLite,
sync, events, blobs, CRDT fields, encryption, conflict resolution, and worker
queues without knowing app table names.

## What gets generated

Generated app code provides the type-safe table layer:

- table metadata and migration-derived schemas
- Rust Diesel row types and Syncular mutation builders
- TypeScript Kysely DB types, mutation helpers, subscriptions, and row deltas
- Swift/Kotlin rows, query DSLs, mutations, subscriptions, CRDT helpers, and
  row-delta helpers

This keeps the framework reusable while still giving app code concrete types.
