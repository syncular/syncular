---
title: Rust
description: Rust client docs for generated Diesel types, mutations, runtime features, testing, and packaging.
---

# Rust

Rust is the canonical Syncular client runtime. Use this section when your app
code is Rust, or when a native shell embeds Syncular through the Rust runtime.
The Rust path is not an ORM layer over Syncular. Reads stay Diesel
query-builder expressions, while writes go through generated Syncular mutation
namespaces so local rows, outbox commits, conflicts, blobs, CRDT fields, and
encryption remain coherent.

## Getting Started

<Cards>
  <Card title="Quick Start" href="/clients/rust/quick-start" description="Generate Rust app code, open the client, query with Diesel, and mutate through Syncular" />
  <Card title="Generated Client" href="/clients/rust/generated-client" description="Understand generated schema, row types, migrations, metadata, subscriptions, and table helpers" />
</Cards>

## Core APIs

<Cards>
  <Card title="Querying" href="/clients/rust/querying" description="Use Diesel expressions through client-owned SQLite connections" />
  <Card title="Mutations" href="/clients/rust/mutations" description="Insert, update, delete, lease, and batch app-table writes through Syncular" />
  <Card title="Live Updates" href="/clients/rust/live-updates" description="Resolve conflicts and refresh typed Diesel live queries from row change reports" />
</Cards>

## Host Integration

There is one Rust runtime; native hosts embed it directly instead of bridging
to it. Swift, Kotlin, JVM, React Native, Tauri, and Electron shells consume
the same engine through FFI bindings, so sync behavior is identical to the
in-process Rust SDK.

<Cards>
  <Card title="Native Hosts" href="/clients/rust/native-hosts" description="Embed the Rust runtime from Swift, Kotlin, JVM, React Native, Tauri, or Electron hosts" />
  <Card title="Packaging" href="/clients/rust/packaging" description="Build native runtime artifacts from the Rust workspace" />
</Cards>

## Advanced Features

<Cards>
  <Card title="Runtime" href="/clients/rust/runtime" description="Run the worker, event stream, queued commands, and UI lifecycle model" />
  <Card title="Realtime And Presence" href="/clients/rust/realtime-presence" description="Use websocket wakeups, reconnect recovery, and presence scope membership" />
  <Card title="CRDT Fields" href="/clients/rust/crdt-fields" description="Persist and materialize Yjs/Yrs-backed fields without editor-specific storage code" />
  <Card title="Blobs And Encryption" href="/clients/rust/blobs-encryption" description="Store files, process upload queues, cache downloads, and configure field/blob encryption" />
</Cards>

## Testing

<Cards>
  <Card title="Testing" href="/clients/rust/testing" description="Use syncular-testkit with real generated clients and SQLite state" />
</Cards>

## Runtime split

| Layer | Rust responsibility |
|---|---|
| Generated app client | Diesel schema, row structs, table metadata, mutation namespaces, subscriptions, changed-row helpers |
| `syncular-client` | Developer-facing Rust SDK for typed reads, generated writes, sync, conflicts, and live queries |
| `syncular-runtime` | SQLite, migrations, outbox, protocol, native worker, event streams, blobs, CRDT fields, encryption, realtime |
| `syncular-testkit` | Real SQLite fixtures, in-memory app server, transports, assertions, native event helpers, conformance data |
| Native bindings | BoltFFI/C ABI surfaces used by Swift, Kotlin, JVM, React Native, Tauri, and Electron hosts |

## Recommended path

1. Start with [Quick Start](/clients/rust/quick-start).
2. Read [Generated Client](/clients/rust/generated-client) before
   changing migrations or `syncular.app.ts`.
3. Use [Querying](/clients/rust/querying) and
   [Mutations](/clients/rust/mutations) for day-to-day app code.
4. Use [Runtime](/clients/rust/runtime) for UI apps and host bridges.
5. Use [Native Hosts](/clients/rust/native-hosts) when Swift, Kotlin,
   JVM, React Native, Tauri, or Electron hosts embed the Rust runtime.
6. Add [Testing](/clients/rust/testing) before building app-specific sync
   behavior.

For browser, React, React Native, Tauri, Electron, and TypeScript testing, use
[JavaScript](/clients/javascript).
