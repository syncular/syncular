---
title: Server Overview
description: Server-side sync infrastructure for Syncular
---

# Server Overview

This page describes the server-side surfaces that make up a Syncular deployment.

Use [Server Getting Started](/server/getting-started) for first-time implementation. Use this section when you need to know which package, route, or extension point owns a piece of server behavior.

## Package map

| Package / subpath | Responsibility |
|---|---|
| `@syncular/server` | Core sync engine, schema helpers, handler factory, pruning/compaction primitives, blob manager |
| `@syncular/server/hono` | Hono route factories, realtime wiring, console routes, OpenAPI handler |
| `@syncular/server/sqlite`, `@syncular/server/postgres` | Dialect-specific schema and SQL behavior |
| `@syncular/server/<driver>` | Optional Kysely runtime driver helpers such as `/bun-sqlite`, `/neon`, and `/pglite` |

## Server lifecycle

1. Your app authenticates a request and produces an actor context.
2. Table handlers resolve allowed scopes for that actor.
3. Sync routes read snapshots or apply operations through handlers.
4. The server records accepted changes in the sync tables.
5. Optional realtime infrastructure wakes subscribed clients so they pull again.

This split is intentional:

- guides in [Server](/server) explain how to wire the server
- pages in [Reference](/reference/server) document the server API surface and extension points

## Sync Tables

Running migrations creates these tables:

| Table | Purpose |
|-------|---------|
| `sync_commits` | Commit log with idempotency keys |
| `sync_changes` | Individual changes within commits (with JSONB scopes) |
| `sync_client_cursors` | Per-client cursor tracking |
| `sync_snapshot_chunks` | Large bootstrap chunk storage |

## Mounted surfaces

These routes are relative to where you mount them.

| Surface | Typical mount | Purpose |
|---|---|---|
| Sync route | `/api/sync` | Combined push/pull entrypoint |
| Snapshot chunks | `/api/sync/snapshot-chunks/:chunkId` | Download paged bootstrap chunks |
| Realtime | `/api/sync/realtime` | WebSocket verified deltas and pull-required wake-ups |
| Blob routes | `/api/sync/blobs/*` | Binary upload/download flow |
| Console routes | `/api/console/*` | Operator dashboards, forensics, and maintenance |

## Core extension points

### Table handlers

Table handlers define what syncs, who can access it, and how rows are read or applied.

- Use [Table Handlers](/server/table-handlers) for patterns and examples.
- Use [Table Handlers Reference](/reference/server/table-handlers-reference) for factory options and interfaces.

### Subscription registry

Subscriptions describe the requested data shape, and the registry resolves them into the effective scopes that can actually be read.

See [Subscription Registry](/reference/server/subscription-registry).

### Dialects

Dialects encapsulate database-specific behavior for schema setup and sync operations.

See [Dialects](/reference/server/dialects).

### Realtime bridge

Realtime is optional. It does not transport row data; it wakes clients so they perform another HTTP pull.

See [Realtime WebSocket](/server/realtime-websocket) for implementation and
[Realtime Bridge](/reference/server/realtime-bridge) for exact broadcaster
interfaces.

### Blob manager

Blob support is separate from core row sync. The server tracks blob metadata and issues authenticated upload/download flows through dedicated routes.

See [Blob Storage](/features/recipes/blobs-and-media).

## Use the right doc

- Implementing a new server: [Server Getting Started](/server/getting-started)
- Designing authorization and row sync behavior: [Table Handlers](/server/table-handlers)
- Wiring Hono routes: [Hono Adapter](/reference/server/hono-adapter)
- Running operators workflows: [Operate](/operate)
- Looking for exact HTTP endpoints: [API Reference](/reference/api)

## Next Steps

- [Table Handlers](/server/table-handlers)
- [Table Handlers Reference](/reference/server/table-handlers-reference)
- [Subscription Registry](/reference/server/subscription-registry)
- [Hono Adapter](/reference/server/hono-adapter)
- [Cloudflare Adapter](/reference/server/cloudflare-adapter)
