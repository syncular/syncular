---
title: CLI
description: The syncular CLI commands - doctor, generate, schema check, ops check, and codegen install - plus the create-syncular-app scaffolder.
---

# CLI

The `syncular` npm package is **CLI-only**. It currently ships these commands:

```text
usage: syncular <command>

commands:
  doctor [--manifest-dir <path>] [--schema-config <path>] [--ops-config <path>] [--skip-ops] [--require-ops] [--json] [--pretty]
  generate [--check] [--manifest-dir <path>] [--migrations-dir <path>] [--rust-output-dir <path>] [--app <path>]
  schema check [--manifest-dir <path>] [--config <path>] [--migrations-dir <path>] [--generated-client <path>] [--generated-server <path>] [--json] [--pretty]
  ops check [--manifest-dir <path>] [--config <path>] [--json] [--pretty]
  codegen install [--version <version>] [--root <path>] [--force]
```

Run it with a global install (`syncular --help`) or without one
(`npx syncular --help`, `bunx syncular --help`). The help output is the
source of truth for available commands and flags.

## Command reference

| Command | What it does |
|---|---|
| `syncular doctor --json` | Run the narrow local readiness checks with one machine-readable result |
| `syncular generate` | Generate the Syncular app handoff (`generated/syncular.codegen.json`) and Rust/TypeScript generated app code |
| `syncular generate --check` | Verify generated output is current without rewriting files (CI) |
| `syncular schema check --json` | Verify generated output, migrations, and generated client/server schema versions before deploy |
| `syncular ops check --json` | Verify machine-readable production runbook evidence before deploy |
| `syncular codegen install` | Install the Rust `syncular-codegen` binary with Cargo into Syncular's tool cache |

See [Doctor Command](/reference/cli/doctor),
[Generate Command](/reference/cli/generate), and
[Ops Check Command](/reference/cli/ops-check) for the full command details.

## Scaffolding new projects

Project scaffolding is a separate package, not a `syncular` subcommand:

```bash
bunx create-syncular-app my-app
```

It scaffolds a runnable local-first app (migrations, `syncular.app.ts`,
server, client). The [Quick Start](/start/quick-start) tours the generated
layout.

## What the CLI does not do

Earlier prototypes shipped `create`, `migrate`, `console`, `dev`, and managed
Spaces commands under `syncular ...`. Those do not exist in the current CLI:

- **Scaffolding** → `bunx create-syncular-app` (above)
- **Migrations** → your app owns its migration runner; see
  [Schema Migrations](/features/migrations)
- **Console** → embed or serve `@syncular/console` assets yourself; see
  [Console Integration](/operate/console-integration)

## Related guides

- [Generate Command](/reference/cli/generate)
- [Doctor Command](/reference/cli/doctor)
- [Ops Check Command](/reference/cli/ops-check)
- [Generated Client](/clients/javascript/generated-client) — what generation produces and when to re-run it
- [Quick Start](/start/quick-start) — scaffold-first walkthrough
