---
title: CLI
description: Generate Syncular app code, scaffold projects, and run local dev flows with the Syncular CLI.
---

# CLI

The app-facing Syncular CLI is published by the `syncular` npm package. This repo owns the core `syncular generate` command for generated app code.

Managed Spaces workflows live outside the core package, but use the same `syncular ...` command shape when that CLI is installed.

In TTY sessions, it opens interactive mode by default. Use `--no-interactive` for script mode.

## Run the CLI

Global install:

```bash
syncular --help
```

Run without global install:

```bash
npx syncular --help
bunx syncular --help
```

The help output is the source of truth for currently available commands and flags.

## Command reference

| Local command | What it does |
|---|---|
| `syncular generate` | Generate Syncular app config and Rust/TypeScript generated app code |
| `syncular generate --check` | Verify generated output is current without rewriting files |
| `syncular doctor` | Run local checks (runtime + workspace + git) |
| `syncular console` | Serve a local Syncular Console instance |
| `syncular create --template syncular-libraries` | Scaffold integration libraries (default template) |
| `syncular create --template syncular-demo` | Scaffold a runnable full demo (Hono + Vite React + Rust-owned SQLite) |
| `syncular create --template spaces-app` | Scaffold a managed Spaces app |
| `syncular create --template spaces-demo` | Scaffold the managed Spaces demo project |
| `syncular dev` | Run local managed app dev server |
| `syncular typegen` | Generate app database TypeScript types |
| `syncular migrate-status` | Show current/target/pending migration versions via adapter |
| `syncular migrate-up` | Apply migrations via adapter |

| Managed Spaces command | What it does |
|---|---|
| `syncular login`, `logout`, `whoami` | Authenticate against the control plane |
| `syncular create-space --name <name>` | Create a managed space |
| `syncular target --set spaces` | Set default target profile |
| `syncular build` | Build deployment artifact |
| `syncular eject` | Generate a deployable worker project |
| `syncular deploy --space <spaceId>` | Deploy to a managed space |
| `syncular verify --space <spaceId>` | Verify deployed runtime |
| `syncular deployments --space <spaceId>` | List deployment history |
| `syncular rollback --space <spaceId> --to <deploymentId>` | Roll back deployment |

## Common flags

- `--interactive`: force interactive TUI mode
- `--no-interactive`: disable interactive mode
- `--help`: print command help
- `--version`: print CLI version

## Guides

<Cards>
  <Card title="Create Commands" href="/reference/cli/create">
    Library scaffolding, demo scaffolding, targets, and server/runtime selection
  </Card>
  <Card title="Generate Command" href="/reference/cli/generate">
    One command for Syncular app config and generated code
  </Card>
  <Card title="Migrate Commands" href="/reference/cli/migrate">
    Adapter contract, config wiring, and migration command flags
  </Card>
  <Card title="Console Command" href="/reference/cli/console">
    Serve a local Console instance and preconfigure API URL/token
  </Card>
</Cards>

## Related guides

- [Installation](/start/installation) for package and runtime selection
- [Common Architectures](/features/recipes) if you want to choose an app shape before scaffolding
- [Schema Migrations](/features/migrations) for the underlying migration model
- [Console Integration](/operate/console-integration) if you are serving Console outside the CLI
