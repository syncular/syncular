---
title: Basic Setup
description: Getting started with Syncular
---

# Basic Setup

This page is intentionally short. The canonical, up-to-date setup lives in:

- [Quick Start](/start/quick-start)
- [Server Setup](/server/setup-with-hono)
- [JavaScript](/clients/javascript) or [Rust](/clients/rust)

## One important convention (URLs)

Syncular’s OpenAPI paths are rooted at `/sync` and `/console/*`. A clean way to use that is:

- Choose an API base prefix (example: `/api`)
- Mount Sync routes at `${API_BASE}/sync`
- Open the generated client with `config.baseUrl: API_BASE`

That makes requests land at:

- `POST ${API_BASE}/sync` (with `push` and/or `pull` in the body)
- `GET  ${API_BASE}/sync/realtime` (WebSocket realtime deltas and recovery wake-ups)

## Next Steps

- [Server Setup](/server/setup-with-hono) - Advanced server configuration
- [JavaScript](/clients/javascript) or [Rust](/clients/rust) - Advanced client configuration
- [Authentication](/learn/scopes) - Securing your data
