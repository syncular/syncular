---
title: What is Syncular?
description: Understanding Syncular's approach to offline-first sync
---

# What is Syncular?

Syncular is a **sync framework** for building offline-first applications. Its
client side is Rust-first: JavaScript, React, Rust, and native hosts talk to
the same Rust-owned local SQLite/sync runtime through generated app APIs. It
handles local storage, incremental sync, conflict detection, authorization, and
real-time updates so you can focus on building your app.

## The problem

Modern apps are expected to work everywhere — on planes, in subways, with spotty connections. But building offline support from scratch means solving:

1. **Local state** — Where does data live when offline?
2. **Sync logic** — How do you reconcile changes when connectivity returns?
3. **Conflicts** — What happens when two users edit the same thing?
4. **Authorization** — Who can see what, and how do you enforce it during sync?
5. **Real-time** — How do you push changes to connected clients?

Most teams underestimate this. They start with REST APIs, add caching, try to patch offline support, and end up with a fragile system that breaks in edge cases.

## Three design decisions

Syncular is built on three opinionated choices that make the whole system simpler:

### 1. Rust-owned SQLite on the client

Your app queries local SQLite, not a remote server. In browser and native
clients, the SQLite database, outbox, cursors, conflict state, blobs, CRDT
fields, encryption metadata, and realtime recovery state are owned by the Rust
runtime. Host code uses the query builder that fits the language: Kysely in
TypeScript and Diesel in Rust. This gives you:

- **Instant queries** — no network round-trips, sub-millisecond reads
- **Full SQL power** — joins, aggregations, complex filters
- **Reliable offline** — data is always available locally

```typescript
// Runs against local SQLite — instant, works offline
const { data: tasks } = useSyncQuery(({ selectFrom }) =>
  selectFrom('tasks')
    .selectAll()
    .where('completed', '=', 0)
    .orderBy('created_at', 'desc')
);
```

### 2. Commit-log sync

Instead of replicating database state (complex), Syncular replicates a **log of commits** (simple).

<Mermaid
  chart={`
flowchart TB
  subgraph Log["Commit log"]
    C1["seq 1 · client A · create task-1"]
    C2["seq 2 · client B · create task-2"]
    C3["seq 3 · client A · update task-1"]
    C4["seq 4 · client B · delete task-2"]
    C1 --> C2 --> C3 --> C4
  end

  Cursor["Client B cursor = 2"] --> C2
  Needs["Next sync needs commits 3 and 4"] --> C3
  Needs --> C4
`}
  caption="Clients track a cursor in the append-only log and only request commits after that point."
/>

Clients track their position in the log (a cursor). To sync, they ask: *"Give me commits after my cursor."* This gives you:

- **Incremental sync** — only fetch what you haven't seen
- **Global ordering** — all clients see changes in the same order
- **Easy debugging** — the log is append-only and immutable
- **Idempotent pushes** — retrying a push returns the same result

### 3. Scope-based authorization

Every change is tagged with **scopes** — key-value pairs extracted automatically from row data:

<Mermaid
  chart={`
flowchart LR
  Row["Row: task t1 owned by user 123"] --> Pattern["Scope pattern: user:{user_id}"]
  Pattern --> Scope["Extracted scope: user_id = 123"]
  Scope --> Change["Stored on the commit change"]
  Change --> Delivery["Only matching subscribers receive it"]
`}
/>

On pull, the server computes **effective scopes** as the intersection of what the client requested and what the user is allowed to access:

<Mermaid
  chart={`
flowchart LR
  Requested["Requested: project A"] --> Effective["Effective: project A"]
  Allowed["Allowed: user 123 · projects A and B"] --> Effective
  Effective --> Delivery["Only project A data is returned"]
`}
/>

This makes authorization **automatic** (extracted from data), **explicit** (stored on every change), **auditable** (queryable in the database), and **composable** (combine user, project, team scopes).

## How it fits together

<Mermaid
  chart={`
flowchart TB
  subgraph Client["Client"]
    direction LR
    AppCode["App UI or host code"] --> ClientQuery["Kysely or Diesel"]
    ClientQuery --> Runtime["Rust client runtime"]
    Runtime --> ClientDb["SQLite"]
    AppCode -->|generated mutations| Outbox["Outbox"]
    ClientDb --> Instant["Changes appear instantly"]
  end

  subgraph Server["Server"]
    direction LR
    Handler["Table handlers"] <--> ServerQuery["Kysely"]
    ServerQuery --> Postgres["Postgres"]
    Postgres --> CommitLog["Append-only commit log"]
  end

  Outbox -->|push over HTTP| Handler
  CommitLog -->|pull over HTTP or websocket delta| Runtime
`}
  caption="Your app always reads local data first. The server owns ordering, validation, and authorization through the commit log."
/>

## Key concepts at a glance

| Concept | What it is |
|---------|-----------|
| **Scopes** | Key-value pairs (`user_id`, `project_id`) auto-extracted from rows. Define who can access each change. |
| **Table handlers** | Server-side definitions of how each table syncs. Clients subscribe by table name. |
| **Subscriptions** | Client requests for data: a table + scope values. Example: `{ table: 'tasks', scopes: { project_id: 'A' } }` |
| **Commits** | Atomic bundles of changes in the commit log. Globally ordered by `commit_seq`. |
| **Cursors** | Client's position in the commit log. Used for incremental sync. |
| **Outbox** | Runtime-owned queue for pending changes. Survives offline and app restarts. |
| **Generated app contract** | Schema, subscriptions, mutations, local scope columns, and binding handoff shared by TypeScript, Rust, and native hosts. |
| **Table handlers** | Server-side definitions that validate writes, build snapshots, extract scopes, and emit sync changes. |

## When to use Syncular

For the full decision guide, see [Is Syncular For Me?](/start/is-syncular-for-me)

**Good fit:**
- Apps that need offline support (field work, mobile, unreliable networks)
- Collaborative features with clear data ownership
- Existing Postgres infrastructure
- Teams that value type safety and debuggability
- Need explicit control over sync behavior and authorization

**Consider alternatives:**
- [Zero](/start/comparison/vs-zero) — reactive queries with minimal setup
- [Jazz](/start/comparison/vs-jazz) — real-time collaborative editing with CRDTs
- [PowerSync](/start/comparison/vs-powersync) — managed service with strong mobile SDKs
- [Electric SQL](/start/comparison/vs-electric) — read-path sync from Postgres

## Next steps

- [Is Syncular For Me?](/start/is-syncular-for-me) — decide whether the trade-offs fit your product
- [Hello World](/start/hello-world) — the smallest end-to-end app
- [How Sync Works](/learn/architecture) — deep dive into the commit log
- [Scopes](/learn/scopes) — understand authorization
- [Comparison](/start/comparison) — how we stack up against alternatives
