{
  "name": "@syncular/docs",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "astro build && node scripts/agent-assets.mjs && node scripts/rebase.mjs",
    "dev": "astro dev",
    "deploy": "astro build && node scripts/agent-assets.mjs && node scripts/rebase.mjs && wrangler deploy"
  },
  "dependencies": {
    "astro": "^7.0.6"
  },
  "devDependencies": {
    "@shikijs/langs": "^4.3.1",
    "shiki": "^4.3.1",
    "wrangler": "^4.105.0"
  }
}
